# Student-Report-Management-System
The Student Report Management System is a simple C-based console application that manages student records using text-file storage. It supports role-based access (Admin, Staff, Guest) and allows adding, viewing, searching, updating, and deleting student details based on user permissions.
